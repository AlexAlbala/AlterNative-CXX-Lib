language: c
 
#whitelist
branches:
  only:
    - master

git:
  submodules: false
  
install:
  - sudo apt-get -f install cmake libboost-all-dev libboost-dev build-essential libboost-chrono1.48-dev libboost-locale1.48-dev 
 
script:
  - mkdir tmp
  - cd tmp
  - git clone https://github.com/AlexAlbala/Alter-Native
  - cd Alter-Native/Tools
  - declare -x ALTERNATIVE_TOOLS_PATH=`pwd`
  - declare -x BOOST_LIBRARYDIR=/usr/lib/
  - cd ../../../
  - source ./alternative-lib-compile.sh
  - rm -rf tmp
  - cd build/libfiles
  - "[[ -f libSystem.a && -f libgc-lib.a ]]"

notifications:
 email: true
